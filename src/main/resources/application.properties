spring.application.name=demo

# Root directory on disk where resource folders are copied (outside the project).
# The copied folder name is appended, e.g. dest-dir=/opt/app/data + "templates" -> /opt/app/data/templates
app.resource-copy.dest-dir=/home/khalil/demo-resource-copy
